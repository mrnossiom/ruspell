//! Logic of the [`Dictionary`] to suggests words

use crate::Dictionary;

#[derive(Debug, thiserror::Error)]
pub enum SuggestError {}

/// Methods for suggesting words based on the dictionary
impl Dictionary {
	// fn suggest() {}
}
